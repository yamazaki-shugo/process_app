  <%= form_with model: @room, class: 'room-make', local: true do |f| %>

  <div class='room-form-wrap'>
    <div class='room-form-header'>
      <h1 class='room-form-header-text'>
        ルーム情報入力
      </h1>
    </div>

    <%= render 'shared/error_messages', model: f.object %>

    <div class="room-form-group">
      <div class='room-form-text-wrap'>
        <span class="indispensable">必須</span>
        <label class="room-form-text">ルーム名</label>
      </div>
      <%= f.text_area :name, class:"input-default", id:"name", autofocus: true %>
    </div>

    <div class="room-form-group">
      <div class='room-form-text-wrap'>
        <span class="indispensable">必須</span>
        <label class="room-form-text">パスワード</label>
      </div>
      <%= f.password_field :password, class:"input-default", id:"password", placeholder:"8文字以上の半角英数字" %>
      <p class='info-text'>※英字と数字の両方を含めて設定してください</p>
    </div>

    <div class="room-form-group">
      <div class='room-form-text-wrap'>
        <span class="indispensable">必須</span>
        <label class="room-form-text">パスワード(確認)</label>
      </div>
      <%= f.password_field :password_confirmation, class:"input-default", id:"password-confirmation", placeholder:"同じパスワードを入力して下さい" %>
    </div>

      <input name="room[user_ids]" type="hidden" value=<%= current_user.id %>>
    <div class="room-form-group">
      <div class='room-form-text-wrap'>  
      </div>  
    </div>
    <div class='register-btn'>
      <%= f.submit "ルーム作成" ,class:"register-red-btn" %>
    </div>

  </div>
<% end %>
  
